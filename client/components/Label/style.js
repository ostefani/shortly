import css from 'styled-jsx/css';

export default css`
    label {
        color: var(--onbackground);
        font-size: var(--large);
        display: block;
        font-family: var(--main);
    }
`;
